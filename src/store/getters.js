export const isLoading = (state) => state.loading;

export const getDroneOrder = (state) => state.order;
export const getListView = (state) => state.listView;
export const getSearchInput = (state) => state.search;

export const getDroneList = (state) => state.drones;
export const currentDrone = (state) => state.drone;